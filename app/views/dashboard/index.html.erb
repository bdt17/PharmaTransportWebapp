<div class="container mt-5">
  <h1 class="text-success">🚚 PharmaTransport Dashboard</h1>
  
  <% @trucks.each do |truck| %>
    <div class="card mb-4">
      <div class="card-body">
        <h5><%= truck.name %> 
          <% if truck.monitorings.last&.status == 'critical' %>
            <span class="badge bg-danger">🚨 POWER FAILURE</span>
          <% end %>
        </h5>
        <p>Voltage: <%= truck.monitorings.last&.voltage || 'N/A' %>V | 
           Temp: <%= truck.monitorings.last&.temp || 'N/A' %>°C</p>
        <%= link_to "🚨 Send SMS Alert", send_sms_alerts_path(truck_id: truck.id), 
            class: "btn btn-warning" %>
      </div>
    </div>
  <% end %>
</div>
